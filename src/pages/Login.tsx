// Trang Login hiện tại không còn dùng làm route chính.
// Giữ lại file để tránh lỗi import, nhưng chỉ hiển thị một thông báo đơn giản.

const Login = () => {
  return null;
};

export default Login;

